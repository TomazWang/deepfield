# {{PROJECT_NAME}} Knowledge Base

Generated by [Deepfield](https://github.com/TomazWang/deepfield) — snapshot `{{SNAPSHOT_VERSION}}` ({{SNAPSHOT_DATE}}).

## Viewing locally

```bash
npm install
npm start
```

Then open http://localhost:3000.

## Building a static site

```bash
npm run build
npm run serve
```

## About this site

This knowledge base was generated from a Deepfield analysis of `{{PROJECT_NAME}}`.
Each section corresponds to a documentation tier:

- **Context** — stakeholder view: what the system does and why
- **Tech Design** — architecture, decisions, risks, and technical debt
- **Implementation** — component-level detail: code internals and contracts

---

*Do not edit files in `docs/` directly — they are regenerated on each `/df-output` run.*
*Edit drafts in `deepfield/drafts/` and re-run `/df-output` to refresh.*
